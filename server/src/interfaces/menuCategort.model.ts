export interface MenuCategory {
    id: number; // Unique identifier for the menu category
    name: string; // Name of the menu category
    imageUrl?: string; // Optional URL for the category image
}

